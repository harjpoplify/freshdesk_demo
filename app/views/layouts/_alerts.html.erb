<% flash.each do |type, message| %>
  <div class="alert alert-dismissible <%= alert_or_notice(type) %>">
    <%= message %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>